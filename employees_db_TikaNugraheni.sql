-- MySQL Script generated by MySQL Workbench
-- Tue Jul  2 18:37:12 2019
-- Model: New Model    Version: 1.0
-- MySQL Workbench Forward Engineering

SET @OLD_UNIQUE_CHECKS=@@UNIQUE_CHECKS, UNIQUE_CHECKS=0;
SET @OLD_FOREIGN_KEY_CHECKS=@@FOREIGN_KEY_CHECKS, FOREIGN_KEY_CHECKS=0;
SET @OLD_SQL_MODE=@@SQL_MODE, SQL_MODE='ONLY_FULL_GROUP_BY,STRICT_TRANS_TABLES,NO_ZERO_IN_DATE,NO_ZERO_DATE,ERROR_FOR_DIVISION_BY_ZERO,NO_ENGINE_SUBSTITUTION';

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------

-- -----------------------------------------------------
-- Schema mydb
-- -----------------------------------------------------
CREATE SCHEMA IF NOT EXISTS `mydb` DEFAULT CHARACTER SET utf8 ;
USE `mydb` ;

-- -----------------------------------------------------
-- Table `mydb`.`employees`
-- -----------------------------------------------------
CREATE TABLE IF  EXISTS `mydb`.`employees` (
  `emp_no` INT(11) NOT NULL,
  `birth_date` DATE NOT NULL,
  `first_name` VARCHAR(14) NOT NULL,
  `last_name` VARCHAR(16) NOT NULL,
  `gender` ENUM('M', 'F') NOT NULL,
  `hire_date` DATE NOT NULL,
  PRIMARY KEY (`emp_no`))
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`departments`
-- -----------------------------------------------------
CREATE TABLE IF  EXISTS `mydb`.`departments` (
  `dept_no` CHAR(4) NOT NULL,
  `dept_name` VARCHAR(40) NOT NULL,
  PRIMARY KEY (`dept_no`),
  INDEX `dept_name` ())
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`dept_emp`
-- -----------------------------------------------------
CREATE TABLE IF EXISTS `mydb`.`dept_emp` (
  `emp_no` INT(11) NOT NULL,
  `dept_no` CHAR(4) NOT NULL,
  `from_date` DATE NOT NULL,
  `to_date` DATE NOT NULL,
  `employees_emp_no` INT NOT NULL,
  `departments_dept_no` CHAR(4) NOT NULL,
  PRIMARY KEY (`emp_no`, `dept_no`, `employees_emp_no`, `departments_dept_no`),
  INDEX `dept_no` (),
  INDEX `emp_no` (`dept_no` ASC),
  CONSTRAINT `emp_no`
    FOREIGN KEY (`emp_no`)
    REFERENCES `mydb`.`employees` (`emp_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `dept_no`
    FOREIGN KEY (`dept_no`)
    REFERENCES `mydb`.`departments` (`dept_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`salaries`
-- -----------------------------------------------------
CREATE TABLE IF  EXISTS `mydb`.`salaries` (
  `emp_no` INT(11) NOT NULL,
  `salary` INT(11) NULL,
  `from_date` DATE NOT NULL,
  `to_date` DATE NOT NULL,
  PRIMARY KEY (`emp_no`, `from_date`),
  INDEX `emp_no` (),
  CONSTRAINT `emp_no`
    FOREIGN KEY (`emp_no`)
    REFERENCES `mydb`.`employees` (`emp_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`titles`
-- -----------------------------------------------------
CREATE TABLE IF EXISTS `mydb`.`titles` (
  `emp_no` INT(11) NOT NULL,
  `title` VARCHAR(50) NOT NULL,
  `from` DATE NOT NULL,
  `to_date` DATE NULL,
  PRIMARY KEY (`emp_no`, `title`, `from`),
  INDEX `emp_no` (),
  CONSTRAINT `emp_no`
    FOREIGN KEY (`emp_no`)
    REFERENCES `mydb`.`employees` (`emp_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


-- -----------------------------------------------------
-- Table `mydb`.`dept_manager`
-- -----------------------------------------------------
CREATE TABLE IF EXISTS `mydb`.`dept_manager` (
  `dept_no` CHAR(4) NOT NULL,
  `emp_no` INT(11) NOT NULL,
  `from_date` DATE NOT NULL,
  `to_date` DATE NOT NULL,
  `departments_dept_no` CHAR(4) NOT NULL,
  `employees_emp_no` INT NOT NULL,
  PRIMARY KEY (`dept_no`, `emp_no`, `departments_dept_no`, `employees_emp_no`),
  INDEX `emp_no` (),
  INDEX `dept_no` (),
  INDEX `emp-_no_idx` (`emp_no` ASC),
  CONSTRAINT `dept_no`
    FOREIGN KEY (`dept_no`)
    REFERENCES `mydb`.`departments` (`dept_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION,
  CONSTRAINT `emp-_no`
    FOREIGN KEY (`emp_no`)
    REFERENCES `mydb`.`employees` (`emp_no`)
    ON DELETE NO ACTION
    ON UPDATE NO ACTION)
ENGINE = InnoDB;


SET SQL_MODE=@OLD_SQL_MODE;
SET FOREIGN_KEY_CHECKS=@OLD_FOREIGN_KEY_CHECKS;
SET UNIQUE_CHECKS=@OLD_UNIQUE_CHECKS;
